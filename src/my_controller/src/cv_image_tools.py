# This is for functions that we think could be useful for both
# the driver and license plate nodes, so we don't have to implement 
# them twice. 

# In other words, this is basically a library for working with cv images.

import cv2
import numpy as np

